export interface ProductDataDTO {
  productId: string        // giống backend
  productText: string      // giống backend
  unitPrice: string        // backend trả string
  image?: string           // có thể undefined
}
